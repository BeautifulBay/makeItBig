timing.py is a script that parse test.txt to kernel and bootloader format and modify at "timings" part of kernel and bootloader file at the same time.
Usage:
	./timing.py test.txt [kernel.dtsi] [uboot.h]
